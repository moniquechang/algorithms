/**
 * @param {string} s
 * @param {string} t
 * @return {character}
 */
var findTheDifference = function (s, t) {
  /* const sArr = s.split('').sort();
  const tArr = t.split('').sort();
  while (sArr) {
    if (sArr[0] !== tArr[0]) {
      return tArr[0];
    } else {
      sArr.splice(0, 1);
      tArr.splice(0, 1);
    }
  } */
};

// function above is commented out for commit

// added for commit
findTheDifference();
